#include <stdio.h>

int main(){

    char st[30];
    gets(st); // used for getitng input 

    // printf("%s", st);
    puts(st); // used to printf
    printf("Hey");
    return 0;

}

// declaring strings using pointer 